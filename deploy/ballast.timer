[Unit]
Description=Ballast — run a trading tick every hour

[Timer]
# First tick 2 min after boot, then every hour. Persistent fires a catch-up
# tick on boot if the box was down when a tick was due.
OnBootSec=2min
OnUnitActiveSec=1h
Persistent=true

[Install]
WantedBy=timers.target
